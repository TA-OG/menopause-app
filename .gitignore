# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER COMMIT THESE
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# android keystore — NEVER COMMIT
android/*.keystore
android/*.jks

# Google OAuth client secret downloads — NEVER COMMIT
client_secret*.json

# Claude internal files
.claude/

# Supabase CLI local link/session state — machine-specific, regenerated by
# `supabase link` / `supabase start`, never something to share via git
supabase/.temp/

# Local ruvector/agentdb tooling database — dev-tooling state, not app data
ruvector.db
